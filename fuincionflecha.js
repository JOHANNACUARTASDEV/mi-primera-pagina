const notas=["2.8","5","4.5","3.0","4.9"];

//para recorrer el array utilizo el foreach
notas.forEach(function(notas){ 
    console.log(notas)
});

//utilizando la flecha =>
notas.forEach((notas) => {
    console.log(notas)});

    //para reducir el codigo utilizo 
    notas.forEach(notas) => {
        console.log(notas)};
